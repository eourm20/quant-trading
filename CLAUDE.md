# Quant Trading System — Claude.ai 행동 지침

## 프로젝트 개요
AI 기반 개인용 퀀트 트레이딩 시스템.
키움증권 MCP로 계좌조회/매매 가능.
백그라운드 워커가 평일 장 시간(08:00~18:00) 자동 조건 감지 → 텔레그램 알림 → 텔레그램 봇으로 주문 실행.

---

## 핵심 원칙: 결정이 나면 즉시 반영

대화 중 아래 상황이 발생하면 사용자가 별도로 지시하지 않아도 자동으로 처리한다.
모든 기록/알림은 MCP 도구를 직접 호출하여 처리한다 (bash 명령 사용 금지).

### 주식 실시간 정보는 반드시 키움 MCP로 조회

**현재가, 잔고, 체결, 주문, 계좌 정보 등 주식 관련 실시간 데이터는 반드시 `kiwoom_execute_api` 도구를 직접 호출하여 확인한다.**
DB(portfolio 테이블)는 동기화 시점의 스냅샷이므로 현재가·수익률 등 실시간 수치는 부정확할 수 있다. 절대로 DB 캐시 값만으로 답하지 말 것.

| 조회 내용 | 키움 TR | 비고 |
|----------|---------|------|
| 종목 현재가 | `ka10001` | 단일 종목 현재가 |
| 일봉 차트 | `ka10081` | N일 OHLCV |
| 계좌 잔고/보유 종목 | `kt00018` | 실시간 잔고 |
| 주문 체결 내역 | `kt00007` | 당일 체결 |

트리거 예시:
- "지금 XX 얼마야?", "현재가 알려줘"
- "지금 수익률 어때?", "평가손익 얼마야?"
- "잔고 얼마나 있어?", "계좌 확인해줘"
- "오늘 체결된 거 있어?"

### 보유 여부 확인이 필요한 경우

특정 종목의 보유 여부가 대화 맥락에서 필요하면 반드시 `quant_report(type="portfolio")`를 먼저 호출하여 확인한다. 추측하거나 "모른다"고 답하지 말 것.

트리거 예시:
- "이 종목 가지고 있어?"
- "에프에스티 미보유지?"
- "XX 종목 지금 몇 주야?"
- 신호 수신 후 보유/미보유 판단이 필요한 모든 경우

### 전략/매매 맥락 파악이 필요한 경우

매매 검토·전략 수립·신호 해석 등 판단이 필요한 대화가 시작되면 `quant_report(type="strategy")`를 자동으로 호출해 최근 전략 노트를 파악한다. 이전 결정과 일관성 있는 의견을 제공하기 위해서다.

트리거 예시:
- 신호 수신 후 매수/매도 검토 시
- "전략 어떻게 가져가면 돼?"
- "지금 시장 어떻게 봐?"
- 종목 매매 근거를 물을 때

---

## 상황 1: 조건 변경 결정

**트리거 예시** (이런 말이 나오면 행동)
- "손절가를 XX원으로 낮출게"
- "이 종목 좀 더 타이트하게 관리하자"
- "RSI 기준을 65로 바꾸는 게 낫겠다"
- "목표가 올려야 할 것 같아"
- "이 종목 모니터링 잠깐 꺼둬"

**자동으로 할 것**
1. `quant_watchlist_update` 도구로 DB 즉시 반영 (텔레그램 발송 없음)
2. 모든 변경이 끝난 후 `quant_strategy_log` 1회 호출로 전체 요약 발송:
   - category: `"watchlist"`
   - summary: 변경 내용 전체 요약 한 줄 (예: "한화에어로 손절 상향, LIG 목표가 하향")
   - detail: 대화에서 나온 변경 이유

---

## 상황 2: 매매 실행

**트리거**: kiwoom MCP로 매수/매도 주문 실행 후

**자동으로 할 것**
1. `quant_cooldown_reset(stock_code=...)` 도구로 해당 종목 쿨다운 리셋:
   - 매매 후 이전 신호 쿨다운이 남아 있으면 새 포지션 기준 신호가 막히기 때문
   - 매수/매도 모두 동일하게 처리
2. `quant_strategy_log` 도구로 전략 노트 기록 + 텔레그램 발송:
   - category: `"trade"`
   - summary: "종목명 N주 매수/매도"
   - detail: 대화에서 나온 매매 근거
3. `quant_portfolio_sync` 도구로 포트폴리오 DB 동기화

---

## 상황 3: 신호 수신 후 매매 검토

**signal_type 구분**
- `entry`: 미보유 종목 → 신규 매수 타이밍
- `exit`: 보유 종목 → 매도·익절·손절 검토
- `add`: 보유 종목 → 물타기·추가매수 검토
- `both`: 보유/미보유 모두 적용

**미보유 종목 entry 신호 수신 시**
1. `quant_report(type="signals", days=1)`로 최근 신호 확인
2. `quant_watchlist_read()`로 해당 종목 목표가/손절가/조건 확인
3. 매수 결정이 나면 → **상황 2** 흐름으로 처리

**보유 종목 exit/add 신호 수신 시**
- `exit` 신호: 매도·홀드 검토
- `add` 신호: 추가매수·물타기 검토
- entry 신호만 발생했다면 워커가 이미 필터링했으므로 알림 미발송 (정상)

**AI 홀드 판단 시 자동 처리 (워커)**

AI가 `[홀드]` 판단 + `[전환조건]` + `[임계값]`을 출력하면 워커가 자동으로:
1. `[전환조건]` 텍스트를 `strategy_notes` 테이블에 저장 (category: `watchlist`)
2. `[임계값]`에 `field=value` 쌍이 있으면 텔레그램으로 변경 제안 메시지 발송

텔레그램 메시지에는 필드별 `[✅ 적용]` `[✏️ 수정]` `[❌ 제외]` 버튼이 포함된다.
- ✅ 적용: `update_stock_field()` 즉시 반영 + 전략 노트 기록
- ✏️ 수정: 새 값 텍스트 입력 대기 → 입력 시 적용
- ❌ / 전체 취소: 무시

허용 임계값 필드: `rsi_oversold`, `rsi_overbought`, `target_price`, `stop_loss_price`

---

## 상황 4: 하루 복기 (daily_review)

장 마감 후 하루를 복기할 때 `quant_strategy_log`로 총평을 저장한다.

**트리거 예시**
- "오늘 복기하자", "오늘 하루 어땠어?", "오늘 총평 저장해줘"
- 장 마감 후 매매·신호·전략 변경을 함께 검토하는 대화

**자동으로 할 것**
1. `quant_report(type="all")`로 오늘 신호·매매·전략 노트 전체 조회
2. 대화에서 나온 총평을 `quant_strategy_log`로 기록:
   - category: `"daily_review"`
   - summary: "YYYY-MM-DD 하루 복기"
   - detail: 시스템 평가, 의사결정 평가, 내일 확인 사항 등

---

## 상황 5: 전략 수립/변경 (전략메모)

**트리거 예시**
- "방산주 비중을 늘리기로 했어"
- "당분간 현금 비중 높게 가져갈게"
- "이번 분기 전략은 XX로 하자"

**자동으로 할 것**
1. `quant_strategy_log` 도구로 전략 노트 기록 + 텔레그램 발송:
   - category: `"general"`
   - summary: 전략 요약
   - detail: 결정 근거
2. 전략 내용에 아래 항목이 포함된 경우 `quant_watchlist_update`로 관심종목에도 즉시 반영:

| 전략 내용 예시 | watchlist 반영 |
|---|---|
| "삼성전자 목표가 90000으로" | `target_price = 90000` |
| "SK하이닉스 손절 83만" | `stop_loss_price = 830000` |
| "한화에어로 RSI 기준 65로" | `rsi_overbought = 65` |
| "LIG 모니터링 꺼둬" | `enabled = false` |
| "이 종목 비중 줄이자" → 목표가/손절가 조정 수반 시 | 해당 필드 업데이트 |

> 수치·종목이 명확하지 않으면 반영하지 말고 사용자에게 확인 후 처리.

---

## 상황 5: 현황 조회 요청

**트리거 예시**
- "오늘 신호 뭐 왔어?", "최근 신호 보여줘"
- "포트폴리오 어때?", "지금 종목 현황은?"
- "최근 매매 내역 보여줘"
- "전략 노트 보여줘"
- "전체 현황 요약해줘"

**자동으로 할 것** (`quant_report` 도구 호출)

| 요청 | 도구 호출 |
|------|----------|
| 오늘 신호 | `quant_report(type="signals", days=1)` |
| 최근 N일 신호 | `quant_report(type="signals", days=N)` |
| 포트폴리오 | `quant_report(type="portfolio")` |
| 최근 매매 내역 | `quant_report(type="trades")` |
| 전략 노트 | `quant_report(type="strategy")` |
| 전체 요약 | `quant_report(type="all")` |

---

## 상황 6: 관심종목/조건 조회 및 변경

| 요청 예시 | 도구 |
|----------|------|
| "관심종목 보여줘", "어떤 종목 보고 있어" | `quant_watchlist_read` |
| "목표가/손절가/RSI 기준 바꿔줘" | `quant_watchlist_update(stock_code, field, value)` |
| "이 종목도 모니터링해줘" | `quant_watchlist_add(code, name, conditions)` |
| "이 종목 감시 목록에서 빼줘" | `quant_watchlist_delete(stock_code)` |
| "어떤 조건으로 신호 보내?" | `quant_conditions_list` |
| "새 조건 추가해줘" | `quant_condition_add(...)` |
| "조건 쿨다운/메시지/설명 바꿔줘" | `quant_condition_update(id, ...)` |
| "조건 정의 삭제해줘" | `quant_condition_remove(id)` |

**신호 로그 (signals 테이블) — 실제 발생한 신호 이력**

> ⚠️ "시그널"이 나오면 반드시 구분할 것:
> - **신호 로그** = 실제 발생한 알림 이력 (`signals` 테이블) → `quant_signal_log_*` 도구
> - **조건 정의** = 신호를 발생시키는 규칙 (`conditions_def` 테이블) → `quant_condition_*` 도구

| 요청 예시 | 도구 |
|----------|------|
| "신호 기록 보여줘", "신호 이력 확인" | `quant_report(type="signals")` |
| "이 종목 신호 이력 지워줘" | `quant_signal_log_delete(stock_code=...)` |
| "특정 신호 1건 삭제" | `quant_signal_log_delete(signal_id=...)` |
| "오래된 신호 정리해줘" | `quant_signal_log_delete(before_date=...)` |
| "신호 로그 전체 삭제" | `quant_signal_log_delete(delete_all=True)` |

**전략 노트 (strategy_notes 테이블)**

> note_id 확인 방법: `quant_report(type="strategy")` 응답에 포함된 id 필드 사용.

| 요청 예시 | 도구 |
|----------|------|
| "전략 노트 보여줘" | `quant_report(type="strategy")` |
| "전략 노트 ID N번 수정해줘" | `quant_strategy_note_update(note_id=N, summary=..., detail=...)` |
| "전략 노트 ID N번 지워줘" | `quant_strategy_note_delete(note_id=N)` |
| "전략 노트 전부 지워줘" | `quant_strategy_note_delete(delete_all=True)` |

---

## horizon (매매 기간) 설정

watchlist의 `horizon` 필드로 종목별 매매 기간을 관리한다.

| 값 | 의미 | 특이사항 |
|----|------|---------|
| `단기` | 단타·당일 포지션 | 5분봉 RSI 추가 계산 (`rsi_oversold_intraday` 평가) |
| `중기` | 스윙 | 일봉 RSI만 사용 |
| `장기` | 장기 보유 | 일봉 RSI만 사용 |
| `` (빈값) | 미설정 | 단기 조건 평가 제외, AI 판단에 기간 미표시 |

- `rsi_oversold_intraday` 조건은 horizon=단기인 종목에만 평가됨
- horizon 변경: `quant_watchlist_update(stock_code, "horizon", "단기")` 형태로 반영
- 전략 논의 시 종목의 horizon을 고려하여 맥락에 맞는 조언 제공

## 주요 구성

- DART 공시 조회: `data/dart_client.py` — 신호 발생 시 자동 공시 조회, AI 판단에 포함
  - MCP 도구: `dart_disclosures` / `dart_company_info` / `dart_financial` / `dart_shareholders` / `dart_periodic_report` / `dart_major_event`
- 종목/조건 설정: DB (`data/trading.db`, SQLite) — MCP 도구로 실시간 반영
- 포트폴리오 동기화: `worker/portfolio_sync.py`
- 리포트 조회: `worker/report.py`
- 외부 API 클라이언트: `worker/clients/` (키움 경량, DART 경량, 뉴스) / `kiwoom_mcp/kiwoom_mcp/` (키움 풀, DART 풀)
- 뉴스 검색: `worker/clients/news_client.py` — 네이버 뉴스 API, AI 판단에 뉴스 컨텍스트 제공
- 자동 스크리닝: `worker/stock_analyzer.py` — 장 마감 후 유망 종목 자동 발굴 (워커 전용)
- 신호 로그: `data/db.py`
- 텔레그램 알림: `notifications/telegram.py` (인라인 버튼 포함)
- 텔레그램 봇 주문: `notifications/telegram_bot.py`

---

## 상황 7: 종목 추천/분석 요청 (Claude Desktop)

**트리거 예시**
- "이 종목 어때?", "XX 편입할 만해?"
- "종목 추천해줘", "뭐 살 만한 거 없어?"
- "XX 분석해줘"

**자동으로 할 것**

### 특정 종목 분석 요청 시
1. `kiwoom_execute_api`로 현재가(`ka10001`) + 일봉(`ka10081`) 조회
2. 차트 지표 직접 분석 (RSI, MA, MACD, 볼린저, 스토캐스틱, CCI, 일목균형표 등)
3. `dart_disclosures` / `dart_financial`로 공시+재무 조회
4. 편입 조건 4가지 (눌림목/저평가/테마미반영/실적개선) 중 2가지 이상 충족 여부 평가
5. 편입 적합 시: 목표가, 손절가, horizon, RSI 기준값 제안
6. 사용자 확인 후 `quant_watchlist_add`로 관심종목 등록

### 종목 추천 요청 시 (막연한 요청)
1. `quant_report(type="portfolio")` + `quant_report(type="strategy")`로 현재 상태 확인
2. 포트폴리오 편중/현금 비중/전략 방향 분석
3. 키움 API로 후보 탐색: `ka10023`(거래량 급증) / `ka10027`(등락률) / `ka10035`(외인 순매수)
4. 후보별 간단 분석 후 편입 적합성 평가 → 사용자에게 제안

> **자동 모드 참고** (`worker/stock_analyzer.py`):
> - 장중 10:00, 13:00 — 거래량 급증 종목 경량 스캔 → 텔레그램 알림만 (watchlist 추가 안 함)
> - 장 마감 15:40 — 거래량 급증 + 눌림목 + 외인 순매수 → AI 풀 분석 → 적합 종목 watchlist 자동 추가 + 텔레그램 알림

## 지표 계산 기준 (worker/indicators.py 기준)

전략 논의·임계값 설정 시 아래 계산 기준을 반드시 참조할 것.
워커가 계산하는 RSI·MA·MACD 등은 **모두 일봉 종가 기준**이며 HTS 분봉 지표와 다를 수 있다.

### 데이터 원천
- 키움 `ka10081` TR로 **일봉 90일치** OHLCV(시가·고가·저가·종가·거래량) 조회
- 현재가는 별도로 `ka10001` TR 조회 (당일 실시간)

### RSI
- **방식**: Wilder's Smoothed RSI (horizon별 기간 차등 적용)
  - 초기 avg_gain/loss: 첫 N봉 단순평균 → 이후 `avg = (avg * (N-1) + 당일값) / N`

| horizon | RSI 기간 | 특성 |
|---------|---------|------|
| `단기` | **7일** | 민감, 빠른 신호 (5분봉 RSI도 별도 계산) |
| `중기` / 미설정 | **14일** (기본값) | 표준, 균형 |
| `장기` | **21일** | 완만, 높은 신뢰도 |

| 조건 | 데이터 | horizon 적용 | 기준값 |
|------|--------|------------|-------|
| `rsi_oversold` | 일봉 90일 종가 | 전체 (기간은 horizon별 차등) | 종목별 38~43 |
| `rsi_oversold_intraday` | 5분봉 30개 | 단기만 | 종목별 30~35 |

- ⚠️ HTS/차트 분봉 RSI와 다를 수 있음. 워커 RSI는 반드시 로그(worker.log)로 확인

### 이동평균 (MA)
- **MA5 / MA20**: 최신 N일 종가 단순평균 (SMA)
- **골든크로스**: 전일 MA5 ≤ MA20 이면서 당일 MA5 > MA20으로 전환되는 순간 1회만 발동
- **데드크로스**: 전일 MA5 ≥ MA20 이면서 당일 MA5 < MA20으로 전환되는 순간 1회만 발동
- **MA20 하향 이탈**: 전일 종가 ≥ MA20 이면서 당일 현재가 < MA20
- **MA5 하향 이탈 / 상향 돌파**: 동일 방식

### MACD
- EMA12 − EMA26 = MACD 라인 (k = 2/(n+1) 방식)
- 시그널 라인: MACD의 EMA9
- **골든/데드크로스**: 전일 vs 당일 라인 비교로 전환 순간만 발동
- 최소 필요 데이터: 35일 (26 + 9)

### 볼린저 밴드
- 기준: MA20 ± 2σ, 표준편차는 **모집단 방식** (÷ 20)
- **상단/하단 돌파**: 전일 밴드 내 → 당일 이탈 순간만 발동
- **Critical 과매도**: 현재가 < 하단 × 0.97 (하단 대비 3% 이상 하락) — 쿨다운 5분, 매 체크마다 재알림

### 스토캐스틱 (%K / %D)
- **%K**: (현재 종가 - 14일 최저가) / (14일 최고가 - 14일 최저가) × 100
- **%D**: %K의 3일 단순이동평균
- **골든크로스**: 전일 %K ≤ %D → 당일 %K > %D (과매도 구간에서 더 유의미)
- **데드크로스**: 전일 %K ≥ %D → 당일 %K < %D (과매수 구간에서 더 유의미)
- 과매수: ≥ 80 / 과매도: ≤ 20

### CCI (Commodity Channel Index)
- **공식**: (전형가 - 전형가 SMA20) / (0.015 × 평균편차)
- 전형가 = (고가 + 저가 + 종가) / 3
- CCI > +100: 과매수, 아래로 반전 시 매도 신호
- CCI < -100: 과매도, 위로 반전 시 매수 신호

### 일목균형표
- **전환선**: 최근 9일 (최고가 + 최저가) / 2
- **기준선**: 최근 26일 (최고가 + 최저가) / 2
- **선행스팬A**: (전환선 + 기준선) / 2
- **선행스팬B**: 최근 52일 (최고가 + 최저가) / 2
- **구름대**: 선행스팬A와 B 사이 영역
- **구름대 두께**: (구름 상단 - 하단) / 현재가 × 100%. 두꺼울수록(>3%) 지지/저항 강함, 얇으면(<1%) 돌파 가능
- 가격 > 구름대: 상승 추세 / 가격 < 구름대: 하락 추세 / 구름대 내부: 중립
- **전환선 골든크로스**: 전환선이 기준선 상향 돌파 → 매수 신호
- **전환선 데드크로스**: 전환선이 기준선 하향 돌파 → 매도 신호
- 최소 필요 데이터: 52일

### OBV (On-Balance Volume)
- 최근 10일 거래량 누적 방향으로 매수세/매도세 판단
- 상승 = 매수세 우위 / 하락 = 매도세 우위 / 횡보

### 캔들 패턴 감지
- 워커가 시가·고가·저가·종가로 자동 감지하는 패턴:
  - **도지**: 시가 ≈ 종가, 전환 가능
  - **망치형**: 하락 후 긴 아랫꼬리 양봉 → 반등 신호
  - **역망치형**: 상승 후 긴 윗꼬리 음봉 → 조정 신호
  - **불리시 엔걸핑**: 음봉 후 감싸는 양봉 → 상승 전환
  - **베어리시 엔걸핑**: 양봉 후 감싸는 음봉 → 하락 전환
  - **하락삼법**: 대음봉 + 작은 양봉 3개 + 대음봉 → 하락 지속

### 다이버전스 (RSI + MACD)
- **RSI 다이버전스**: 10일 전 대비 가격 vs RSI 방향 비교 (최소 25일)
- **MACD 다이버전스**: 10일 전 대비 가격 vs MACD 라인 방향 비교 (최소 45일)
- **강세 다이버전스**: 가격 ↓ + 지표 ↑ → 반등 가능
- **약세 다이버전스**: 가격 ↑ + 지표 ↓ → 조정 가능
- RSI + MACD 동시 다이버전스 = 강한 전환 신호

### 볼륨 스프레드 심화 (몸통 × 거래량)
- 캔들 몸통 크기(body_ratio = 몸통/전체범위)와 거래량 비율 조합 분석
- **큰 몸통 + 높은 거래량**: 강한 추세 (양봉=매수세, 음봉=매도세)
- **작은 몸통 + 높은 거래량**: 매수/매도 갈등, 추세 전환 가능
- **큰 몸통 + 낮은 거래량**: 속임수 가능, 신뢰도 낮음
- **작은 몸통 + 낮은 거래량**: 관망세, 방향 미정

### 거래량 추세 (다일간)
- 최근 5일 가격 방향 + 거래량 방향 조합으로 추세 강도 평가
- **가격↑ + 거래량↑**: 추세 강화 (매수세 지속)
- **가격↑ + 거래량↓**: 추세 약화 (매수세 약화 주의)
- **가격↓ + 거래량↑**: 추세 강화 (매도세 강화)
- **가격↓ + 거래량↓**: 추세 약화 (매도압력 약화, 반등 가능)

### 피보나치 되돌림/확장
- 최근 20일 스윙 고점/저점 기준 레벨 자동 계산
- **되돌림**: 23.6% / 38.2% / 50% / 61.8% / 78.6%
- **확장** (목표가 설정용): 127.2% / 161.8% / 200%
- AI 판단 시 현재가 근접 되돌림 레벨 + 확장 레벨 표시

### 차트 패턴 감지
- 90일 데이터에서 스윙 포인트 기반 자동 감지:
  - **이중 바닥/천장**: 유사 저점·고점 2개 + 넥라인
  - **헤드 앤 숄더 / 역 헤드 앤 숄더**: 3개 스윙 포인트 구조
  - **삼각형 (대칭/상승/하락)**: 고점·저점 수렴 방향
  - **깃발형**: 강한 추세 후 작은 역방향 조정 (추세 연장 가능)
  - **쐐기형 (상승/하락)**: 고점·저점 모두 같은 방향 수렴 (반대 돌파 가능)
  - **컵 위드 핸들**: U자형 회복 + 소폭 풀백 (돌파 시 강한 상승)
  - **박스권**: 20일 내 5% 이내 횡보

### 지지/저항
- 최근 20일 고가 최대값 = 저항선
- 최근 20일 저가 최소값 = 지지선
- AI 판단 시 현재가 대비 거리(%) 표시

### 거래량 비율 (volume_surge_ratio)
- `당일 거래량 / 직전 20일 평균` (최신순 index 1~20 평균)
- 최소 필요 데이터: 21일

### 신호 발동 방식 구분
| 유형 | 해당 조건 | 발동 조건 |
|------|----------|----------|
| **연속형** | RSI, 가격, 거래량, CCI | 기준 충족 중이면 쿨다운마다 재발동 |
| **전환형 (flag)** | 골든/데드크로스, MA 이탈, MACD 크로스, 볼린저 돌파, 스토캐스틱 크로스, 일목 전환선 크로스, 일목 구름대 돌파/이탈 | 전일→당일 상태 변화 순간 1회만 발동 |

### AI 판단 프롬프트 구조
- **시스템 프롬프트 (캐싱)**: 트레이딩 지식 베이스 17개 섹션 (캔들·거래량·추세·지지저항·MA·RSI·스토캐스틱·CCI·일목균형표·차트패턴·피보나치·포지션운용·손익비·리스크관리·멘탈관리·시장이해·판단원칙) + 운용 원칙 + 판단 기준 + 출력 형식
- **유저 프롬프트 (매번)**: 신호 데이터·차트 분석 (MA/RSI/스토캐스틱/CCI/일목균형표/OBV/볼륨스프레드/거래량추세/캔들패턴/차트패턴/다이버전스/피보나치/지지저항)·포트폴리오·시장 환경
- Anthropic 프롬프트 캐싱(`cache_control: ephemeral`) 적용으로 반복 호출 시 시스템 프롬프트 토큰 비용 90% 절감

### 신호 조건 (conditions_def) — 총 29개
기존 21개 + 신규 8개:
- `stochastic_golden_cross` / `stochastic_death_cross` — 스토캐스틱 %K/%D 교차
- `cci_oversold` / `cci_overbought` — CCI ±100 기준 과매도/과매수
- `ichimoku_golden_cross` / `ichimoku_death_cross` — 일목 전환선/기준선 교차
- `ichimoku_cloud_breakout` / `ichimoku_cloud_breakdown` — 일목 구름대 돌파/이탈
- 새 조건은 종목별 watchlist에서 해당 필드를 활성화해야 트리거됨 (기본 비활성)

---

---

## 전략 변경 원칙 (2026-03-19 제정)

조건 변경 요청이 들어오면 아래 원칙에 비춰 판단하고, 원칙 위반 가능성이 있으면 먼저 지적한 후 처리한다.

### 손절가 (stop_loss_price)
- **진입 시 확정. 이후 원칙적으로 변경 금지.**
- 허용: 평단가보다 높은 역전 구조 수정 / 수익 구간 진입 후 트레일링 스탑 상향
- 금지: 손절가에 가까워졌을 때 손실 회피 목적 하향 / 버티기 위한 연속 수정
- 기준: 평단 대비 -5~-10% 수준으로 진입 시 확정

### 목표가 (target_price)
- **진입 시 확정. 이후 근거 있는 경우에만 변경.**
- 허용: 대형 호재·수주·실적 서프라이즈 등 펀더멘털 변화 / 애널리스트 목표가 대폭 상향 + 실적 근거
- 금지: 막연한 기대감으로 상향 / 주가가 안 오른다고 포기하여 하향 / 욕심으로 연속 상향
- 목표가 도달 시 익절 실행 원칙 — 홀딩 연장 시 근거 명시 필수

### RSI 기준값 (rsi_oversold / rsi_overbought)
- **진입 전략 수립 시 확정. 신호 빈도 조절 목적의 변경 금지.**
- 허용: 워커 계산 방식 변경 시 / 처음 설정이 일봉/분봉 혼동 등 구조적 오류였을 때
- 금지: 신호가 너무 많아서 올리기 / 신호가 안 와서 내리기 / 시장 분위기에 따라 자의적 조정
- 기준: 일봉 RSI 14기간, 과매도 38~43 / 과매수 60~75 범위 내 설정

### 물타기
- **최대 1회. 절대 변경 금지.**
- 트리거: 평단 -8% 도달 + rsi_oversold 또는 bollinger_lower_break 신호
- 금지 조건: death_cross·MA20 하향이탈·섹터 악재 동반 시

### 신규 종목 편입
- 편입 조건 4가지 중 2가지 이상 충족: 눌림목 / 저평가 / 테마미반영 / 실적개선
- 금지: 단순히 주가가 많이 올랐다는 이유만으로 편입

### 공통
- "시장이 내 예상과 달라서"가 이유면 변경 금지
- "처음 설계가 구조적으로 잘못됐거나 외부 조건이 구조적으로 변했을 때"만 변경
- 모든 변경은 전략 로그에 근거 기록 필수
- 감정적 판단(공포·욕심)으로 인한 변경은 하루 숙려 후 결정 권고
- 3단계 의사결정 원칙: 1차 워커(신호) → 2차 AI(판단) → 3차 사람(최종결정)

---

## signals 테이블 (RAG/학습용 확장)

신호 발생 시 전체 컨텍스트를 저장하여 향후 RAG 검색 및 AI 학습에 활용.

| 컬럼 | 타입 | 용도 |
|------|------|------|
| `verdict` | TEXT | 구조화된 판정 (매수/매도/홀드, claude_opinion에서 자동 추출) |
| `indicator_snapshot` | TEXT (JSON) | 신호 시점 전체 지표 스냅샷 (RSI, MA, 스토캐스틱, CCI, 일목균형표, 다이버전스 등) |
| `dart_summary` | TEXT | 신호 시점 DART 공시 + 재무지표 요약 |
| `chart_patterns` | TEXT (JSON) | 감지된 캔들 패턴 + 차트 패턴 |
| `result_1d` | REAL | 1일 후 수익률 (%) |
| `result_pct` | REAL | 3일 후 수익률 (%) (기존) |
| `result_5d` | REAL | 5일 후 수익률 (%) |
| `result_10d` | REAL | 10일 후 수익률 (%) |

- 결과 수익률은 스케줄러가 자동 업데이트 (30분마다, 장 시간)
- `indicator_snapshot`은 None 값 제거 후 JSON 저장 (용량 절감)
- RAG 검색: "비슷한 지표 상태에서 AI가 어떤 판단을 했고 결과가 어땠는지" 조회 가능

---

## 주의사항
- DB 변경은 워커 재시작 없이 60초 내 자동 반영
- 쿨다운은 매일 09:00 장 시작 시 전체 리셋
- 매매 전 `KIWOOM_ALLOW_TRADE_EXECUTION` 설정 확인 (`true`여야 실제 주문 가능)
- `.env` 내용 절대 출력 금지
